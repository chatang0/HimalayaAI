import type { ApiResponse, ContactFormData, LabProfile, Project } from "../../../shared/types";

/**
 * Re-export shared types for convenient frontend imports.
 */
export type { ApiResponse, ContactFormData, LabProfile, Project };

